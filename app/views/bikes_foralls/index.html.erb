<p id="notice"><%= notice %></p>

<h1>Bikes Foralls</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @bikes_foralls.each do |bikes_forall| %>
      <tr>
        <td><%= link_to 'Show', bikes_forall %></td>
        <td><%= link_to 'Edit', edit_bikes_forall_path(bikes_forall) %></td>
        <td><%= link_to 'Destroy', bikes_forall, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Bikes Forall', new_bikes_forall_path %>
